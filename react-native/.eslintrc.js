module.exports = {
    root: true,
    extends: '@react-native',
    rules: {
        // @react-native config enables prettier/prettier; plugin expects a compatible prettier API.
        'prettier/prettier': 'off',
    },
};
